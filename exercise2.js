// Lista de nombres
var miLista = ["velma", "exploradora", "jane", "john", "harry"];

// Inicializar el contador
var contador = 0;

// Bucle while con un contador para imprimir nombres
while (contador < miLista.length) {
    console.log(miLista[contador]);
    contador++;
}
